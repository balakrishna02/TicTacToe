const modeMsg = document.querySelector("#mode");
const body = document.querySelector("body");

// modeMsg.forEach((mode) => {
    // modeMsg.addEventListener("click", () => {
    //     body.style.backgroundColor = "black";
    //     body.style.color = white;
    })
// })